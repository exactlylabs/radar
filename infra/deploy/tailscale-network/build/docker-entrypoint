#!/usr/bin/env sh

# Load Docker Secrets as variables
if [ -d /var/run/secrets ]; then
  for secret in $(ls /var/run/secrets); do
    export "${secret}"="$(cat /var/run/secrets/${secret})"
  done
fi


exec $@

