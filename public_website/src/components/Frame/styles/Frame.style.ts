import {CSSProperties} from "react";

const frameStyle: CSSProperties = {
  width: '100vw',
  height: '100vh',
  overflowX: 'hidden',
  overflowY: 'auto',
}

export const styles = {
  Frame: frameStyle,
}