import {CSSProperties} from "react";

const iconStyle: CSSProperties = {
  width: '20px',
  height: '20px'
}

const redirectIconStyle: CSSProperties = {
  width: '14px',
  height: 'auto',
  marginLeft: '6px'
}

export const commonStyles = {
  Icon: iconStyle,
  RedirectIcon: redirectIconStyle,
}