import {CSSProperties} from "react";

const mobilePageStyle: CSSProperties = {
  width: '100vw'
}

const mobilePageContentStyle: CSSProperties = {
  width: '90%',
  maxWidth: '1200px',
  margin: '0 auto',
}

export const styles = {
  MobilePage: mobilePageStyle,
  MobilePageContent: mobilePageContentStyle,
}