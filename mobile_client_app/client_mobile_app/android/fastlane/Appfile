json_key_file("./android-secret") # Path to the json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
package_name("com.exactlylabs.radar")

for_platform :android do
    for_lane :dev do
        package_name("com.exactlylabs.radar.dev")
    end
    for_lane :staging do
        package_name("com.exactlylabs.radar.staging")
    end
end