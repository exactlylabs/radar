<%= render partial: "application/components/modals/modal_header",
           locals: { title: "Create a network", is_custom: true, goes_back: true, go_back_url: onboarding_step_2_path, go_back_action: "click->onboard#goToOnboardingStep" }
%>
<%= form_with model: @location, class: "networks--modal-form", data: { controller: "location-form", action: "handlePinMoved@window->location-form#handlePinMoved" }  do |form| %>
  <%= render partial: "networks/modals/shared/network_details_modal",
             locals: {
               type: "create",
               form: form,
               is_onboarding: true,
               location: @location,
             }
  %>
<% end %>