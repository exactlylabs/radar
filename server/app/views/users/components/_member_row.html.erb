<% if type == UsersAccount.name %>
  <%= render partial: "users/components/user_row", 
      locals: {
        row: row,
        widths: widths,
        even: even
      }
  %>
<% else %>
  <%= render partial: "users/components/invitee_row", 
      locals: {
        row: row,
        widths: widths,
        even: even
      }
  %>
<% end %>