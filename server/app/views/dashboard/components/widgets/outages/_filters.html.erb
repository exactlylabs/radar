<% outage_type = params[:outage_type] || 'all' %>

<div class="dashboard--outages-filters-container">
  <a class="dashboard--outages-filter"
     data-selected="<%= outage_type === 'all' %>"
     href="<%= dashboard_path(request.query_parameters.merge({ outage_type: 'all' })) %>"
     target="_top"
  >All outages</a>
  <a class="dashboard--outages-filter"
     data-selected="<%= outage_type === 'isp' %>"
     href="<%= dashboard_path(request.query_parameters.merge({ outage_type: 'isp' })) %>"
     target="_top"
  >ISP outages</a>
  <a class="dashboard--outages-filter"
     data-selected="<%= outage_type === 'power' %>"
     href="<%= dashboard_path(request.query_parameters.merge({ outage_type: 'power' })) %>"
     target="_top"
  >Power outages</a>
  <a class="dashboard--outages-filter"
     data-selected="<%= outage_type === 'pod' %>"
     href="<%= dashboard_path(request.query_parameters.merge({ outage_type: 'pod' })) %>"
     target="_top"
  >Pod failure</a>
</div>