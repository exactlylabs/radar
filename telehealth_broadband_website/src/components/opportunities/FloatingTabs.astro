---
const { ids } = Astro.props;
---

<div
  style="position: sticky; top: 2rem; padding-bottom: 100px; width: max-content; margin: 0 auto; z-index: 10">
  {ids.map((id: string) => (
    <a style="scroll-margin-top: 3rem;" href={`#${id}`}>{id}</a>
  ))}
</div>